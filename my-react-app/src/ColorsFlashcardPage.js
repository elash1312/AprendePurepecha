/*import React from 'react';
import Flashcard from './Flashcard'; // Import the Flashcard component
import ColorsFlashcards from './ColorsFlashcards'; // Import the color flashcard data

const ColorsFlashcardPage = () => {
  return (
    <div>
      <h1>Color Flashcards</h1>
      {ColorsFlashcards.map((flashcard, index) => (
        <Flashcard
          key={index}
          question={flashcard.question}
          answer={flashcard.answer}
          imageName={flashcard.imageName}
          choices={flashcard.choices}
        />
      ))}
    </div>
  );
};

export default ColorsFlashcardPage;
*/
